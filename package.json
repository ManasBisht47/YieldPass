{
  "name": "yieldpass",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "compile": "turbo run compile"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
